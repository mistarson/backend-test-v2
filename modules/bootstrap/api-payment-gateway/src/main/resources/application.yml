spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
server:
  port: 8080

logging:
  level:
    root: INFO
    im.bigs.pg: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,metrics

